package com.liming.controller;

import java.io.IOException;
import java.util.Map;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;

import com.example.demo.domain.ScheduleDomain;
import com.example.demo.thread.ScheduleThread;

/**
 * @author ALPHA.GJ
 * @date 创建时间：2019年3月25日 下午6:29:26
 * @version 1.0
 **/
@Controller
public class JobController {

	/*@RequestMapping("/job/addTask")
	@ResponseBody
	protected Map addTask(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {

		String type = request.getParameter("type");
		String taskid = request.getParameter("taskid");
		ScheduleDomain sche = new ScheduleDomain(type);
		sche.getSche_list().get(0).put("state", 1);
		sche.getSche_list().get(0).put("taskid", taskid);

		boolean succ = SqlUtils.prepareInsert(sche.getSche_list(), sche.getHeaders(), sche.getSche_table(), null);
		sche.getSche_list().get(0).put("succ", succ);

		ScheduleThread st = new ScheduleThread();
		st.start();
		return sche.getSche_list().get(0);

		return null;
	}*/

}
